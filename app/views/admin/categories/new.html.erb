<h1>Create Category</h1>

<%#= form_for(:category, url: admin_categories_path) do |f| %>
<%= form_for([:admin, @category]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
